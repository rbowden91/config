# set PATH so it includes user's private bins, if they exist
if [ -d "$HOME/bin" ] ; then
    export PATH="$HOME/bin:$PATH"
fi
if [ -d "$HOME/.local/bin" ] ; then
    export PATH="$HOME/.local/bin:$PATH"
fi

export EDITOR='vim'
export TERMINAL='termite'

export BINARY_SSH="/usr/bin/ssh.ssh-ident"
export DIR_AGENTS="$HOME/.ssh_agents"
export SSH_DEFAULT_OPTIONS='-A'

# necessary so that gpg appropriately asks for password when SSH'd into the machine
export GPG_TTY=$(tty) export PINENTRY_USER_DATA="USE_CURSES=1"

#alias db='cd "$HOME/Dropbox (CS50)"'

function push () {
    curl -s -F "token=a42feki7f68hvnqnqwpu7bjwjrv5fx" \\
	-F "user=uoo537r5jdq62sg8p545oa4vgbd3gs" \\
	-F "title=YOUR_TITLE_HERE" \\
	-F "message=$1" https://api.pushover.net/1/messages.json
}
export TERM='xterm-256color'
# reduce delay between hitting escape and transitioning modes
export KEYTIMEOUT=1

export BROWSER='google-chrome'

alias utils='cd ~/repos/qls/qls_py/utils/qls/utils/'
alias db='cd ~/repos/qls/qls_py/db/qls/db/'
alias ml='cd ~/repos/qls/qls_py/ml/qls/ml/'
alias stats='cd ~/repos/qls/qls_py/stats/qls/stats/'
alias data='cd ~/repos/qls/qls_py/db/data/'
alias api='cd ~/repos/qls/qls_py/db/api_docs/'
#alias qls='cd ~/repos/qls/'
alias qls_py='cd ~/repos/qls/qls_py/'

alias jupyter_convert='jupyter nbconvert --to python'

